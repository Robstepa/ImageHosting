{% extends 'index.jinja2' %}

{% block content %}
    {% for photo in photos %}
        {% if photo.image %}
            <img src="{{ photo.image.url }}" height="100" width="100">
        {% endif %}
    {% endfor %}
{% endblock %}